<div class="row">
    <div class="col-xs-12">
        <div class="alert alert-danger alert-dismissable" style="display:@( ViewBag.Error == null || ViewBag.Error == false ? new MvcHtmlString("none"): new MvcHtmlString("block"))">
            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
            <h4><i class="icon fa fa-ban"></i> Error!</h4>
            @ViewBag.ErrorMensaje
        </div>
    </div>
</div>
<div class="row">
    <div class="col-xs-12">
        <div class="alert alert-warning alert-dismissable" style="display:@( ViewBag.Alert == null || ViewBag.Alert == false ? new MvcHtmlString("none"): new MvcHtmlString("block"))">
            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
            <h4><i class="icon fa fa-warning"></i> Alerta!</h4>
            @ViewBag.AlertaMensaje
        </div>
    </div>
</div>
<div class="row">
    <div class="col-xs-12">
        <div class="alert alert-info alert-dismissable" style="display:@( ViewBag.Info == null || ViewBag.Info == false ? new MvcHtmlString("none"): new MvcHtmlString("block"))">
            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
            <h4><i class="icon fa fa-info"></i> Información</h4>
            @ViewBag.InfoMensaje
        </div>
    </div>
</div>
<div class="row">
    <div class="col-xs-12">
        <div class="alert alert-success alert-dismissable" style="display:@( ViewBag.Ok == null || ViewBag.Ok == false ? new MvcHtmlString("none"): new MvcHtmlString("block"))">
            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
            <h4>	<i class="icon fa fa-check"></i> OK!</h4>
            @ViewBag.OkMensaje
        </div>
    </div>
</div>
